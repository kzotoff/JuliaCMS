Общая разметка админки:
Кнопки сверху (черные) - фиксированный див .admin_box_main
Чуть пониже - бокс с кнопками админки уже конкретного модуля .admin_buttons
Еще ниже - содержимое админки (менюшки, таблички и прочее) .admin_content

Дописать в мануал:
	поля page/link в menu
	назначение catalog_group.html в модуле меню
	про модуль _sample
	про модуль auth
	<template type="auth" mode="login_message" />
	
Дописать себе:
	годный регэксп для поиска <template>
	$re = '~<template(?=[^>]*?type="auth"[^>]*?)(?=[^>]*?mode="login_message"[^>]*?)[^>]*\s?(/>|>.*?</template>)~';
